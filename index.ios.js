/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';

import XCUITest from './ui/app'

AppRegistry.registerComponent('XCUITest', () => XCUITest);
